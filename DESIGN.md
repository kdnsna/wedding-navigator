# 婚礼导航小程序 — 设计方案

> 版本：v1.0  
> 目标：将现有静态网页升级为微信小程序，建立"主人编辑端 + 客人展示端"双角色体系

---

## 一、核心定位

```
┌─────────────────────────────────────────────────────────────────┐
│                      婚礼导航小程序                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   【主人端】                              【客人端】              │
│   ┌─────────────────────┐               ┌─────────────────────┐ │
│   │  婚书编辑器           │               │  精美请柬展示页      │ │
│   │  婚纱照片上传/管理     │ ──分享──▶     │  婚纱照片瀑布流      │ │
│   │  婚礼信息编辑         │               │  婚礼时间地点信息    │ │
│   │  宾客管理/RSVP统计    │               │  RSVP一键回执       │ │
│   │  地图路线配置         │               │  一键导航           │ │
│   │  实时预览             │               │  祝福留言板         │ │
│   └─────────────────────┘               └─────────────────────┘ │
│                                                                 │
│   一个入口：进入时通过密码/微信绑定判断身份                      │
│   一条链路：编辑完成 → 生成分享卡片/小程序码 → 客人进入展示页    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 双角色架构

| 维度 | 主人端（新郎/新娘） | 客人端（被分享者） |
|------|---------------------|---------------------|
| **入口方式** | 密码验证 / 微信绑定 | 分享卡片 / 小程序码 / 链接 |
| **核心诉求** | 编辑内容、管理数据、查看统计 | 获取信息、确认出席、导航到场 |
| **页面风格** | 工具化、功能密集、可编辑 | 精美展示、沉浸浏览、轻交互 |
| **数据权限** | 读写全部 | 只读 + RSVP提交 |
| **常驻页面** | 工作台（5个Tab） | 请柬展示页（单页长滚动） |

---

## 二、技术架构

### 2.1 技术选型

| 层次 | 方案 | 理由 |
|------|------|------|
| **框架** | 微信小程序原生 | 零配置、微信生态原生支持、分享链路最顺畅 |
| **UI 框架** | Vant Weapp | 成熟的移动端组件库，与小程序原生兼容好 |
| **样式方案** | WXSS + CSS 变量 | 小程序原生支持，保留当前设计系统资产 |
| **状态管理** | MobX-miniprogram | 跨页面状态共享，主人端数据实时同步 |
| **云开发** | 微信云开发（CloudBase） | 免费额度够用，免运维，内置数据库+存储+云函数 |
| **地图** | 腾讯位置服务 | 小程序内原生唤起地图导航，体验最佳 |
| **图片存储** | 云存储 COS | 婚纱照片、头像等静态资源 |

### 2.2 为什么不继续用静态网页？

| 能力 | 静态网页 | 微信小程序 |
|------|----------|------------|
| 微信分享 | H5 链接，卡片样式不可控 | 可自定义分享卡片标题/图片 |
| 扫码进入 | 需要配置域名/二维码 | 天然支持小程序码 |
| 照片上传 | 需要自建上传服务 | 云存储 SDK 一行代码 |
| 数据持久化 | localStorage（易丢失） | 云数据库（可靠持久） |
| 一键导航 | 唤起第三方App | 内置地图组件，无缝导航 |
| 用户体验 | 需浏览器打开 | 原生体验，下拉刷新、手势返回 |
| RSVP收集 | 无后端支持困难 | 云数据库直接写入 |

### 2.3 云开发数据模型

```
cloudbase/
├── database/
│   ├── weddings/          # 婚礼主表（一条记录对应一场婚礼）
│   │   ├── _id
│   │   ├── coupleName     # 新人姓名
│   │   ├── weddingDate    # 婚礼日期
│   │   ├── weddingTime    # 仪式时间
│   │   ├── venue          # 场地信息 {name, address, lat, lng}
│   │   ├── coverImage     # 封面图 cloudID
│   │   ├── gallery        # 照片墙 [{url, caption, order}]
│   │   ├── invitationStyle # 请柬模板风格
│   │   ├── invitationText  # 请柬文案
│   │   ├── timeline       # 流程时间线
│   │   ├── routes         # 路线点位
│   │   ├── ownerOpenId    # 主人微信OpenID
│   │   ├── accessCode     # 访问密码（6位数字）
│   │   ├── createdAt
│   │   └── updatedAt
│   │
│   └── rsvps/             # RSVP 回执表
│       ├── _id
│       ├── weddingId      # 关联婚礼ID
│       ├── guestName
│       ├── phone
│       ├── attendCount    # 出席人数
│       ├── attendStatus   # attending / declined / pending
│       ├── diet           # 饮食偏好数组
│       ├── message        # 祝福留言
│       └── createdAt
│
└── storage/
    └── wedding-photos/    # 照片存储桶
```

---

## 三、页面结构设计

### 3.1 主人端页面（5 Tab 工作台）

```
┌──────────────────────────────────────────┐
│            工作台（TabBar）                │
├────────┬────────┬────────┬────────┬─────┤
│  编辑   │  照片   │  预览   │  宾客   │ 我的 │
│  📝    │  📷    │  👁    │  👥    │ ⚙️  │
└────────┴────────┴────────┴────────┴─────┘
```

| 页面 | 功能 | 核心交互 |
|------|------|----------|
| **pages/editor/index** | 婚书编辑工作台 | 模板切换、文案编辑、婚礼信息表单、实时预览浮窗 |
| **pages/gallery/manage** | 照片墙管理 | 上传/拖拽排序/删除/设置封面、批量管理 |
| **pages/preview/index** | 实时预览 | 以客人视角预览最终效果，支持切换设备尺寸 |
| **pages/guests/index** | 宾客与RSVP | RSVP列表、出席统计图表、导出Excel、发送提醒 |
| **pages/mine/index** | 我的设置 | 分享卡片生成、小程序码下载、访问密码设置、帮助 |

#### 编辑页（核心）详细结构

```
编辑页
├── 顶部：模板切换器（传统/现代/极简 三卡片横滑）
├── 婚书实时预览区（占屏60%，可上下滑动）
├── 编辑抽屉（底部滑出，分Tab：新人/时间/地点/文案/音乐）
│   ├── 新人信息：姓名输入、头像上传
│   ├── 时间地点：日期选择器、时间选择器、地点搜索
│   ├── 请柬文案：6种预设文案 + 自定义文本域
│   ├── 背景音乐：音乐选择器（预设5首 + 上传）
│   └── 高级设置：字体大小、间距微调
└── 底部：保存按钮 + 立即预览按钮
```

### 3.2 客人端页面（单页长滚动 + 4个浮层）

客人端设计理念：**一张精美的电子请柬 = 所有信息**

采用长滚动单页设计，像精美的 H5 请柬一样流畅浏览，同时保留小程序的原生能力。

```
pages/invitation/show?id=WEDDING_ID

长滚动页面结构：
┌─────────────────────────────────────┐
│  Section 1: 封面                    │
│  - 全屏婚纱封面图                    │
│  - 新人姓名 + 婚礼日期               │
│  - 向下滚动提示动画                  │
├─────────────────────────────────────┤
│  Section 2: 婚书/请柬               │
│  - 选中的模板风格展示                │
│  - 文案、时间、地点信息              │
├─────────────────────────────────────┤
│  Section 3: 照片墙                  │
│  - 瀑布流/网格布局                   │
│  - 点击放大预览（图片轮播）          │
├─────────────────────────────────────┤
│  Section 4: 婚礼信息卡片             │
│  - 日期/时间/地点/地址               │
│  - 一键添加到日历                    │
│  - 一键导航（唤起腾讯地图）          │
├─────────────────────────────────────┤
│  Section 5: RSVP回执                │
│  - 是否出席选择                      │
│  - 出席人数步进器                    │
│  - 姓名/电话输入                     │
│  - 祝福留言                          │
│  - 提交按钮                          │
├─────────────────────────────────────┤
│  Section 6: 祝福留言板               │
│  - 已提交的祝福瀑布流                │
│  - 实时弹幕/卡片式展示               │
├─────────────────────────────────────┤
│  Section 7: 底部                     │
│  - 感谢语                            │
│  - 再次分享按钮                      │
│  - "我也要制作"引流入口              │
└─────────────────────────────────────┘

常驻浮层：
- 右上角音乐播放/暂停按钮（如果有背景音乐）
- 底部"我要出席"快捷按钮（跳转到RSVP区域）
```

---

## 四、关键交互设计

### 4.1 分享链路（核心）

```
主人完成编辑
    │
    ▼
点击「分享请柬」
    │
    ├── 生成自定义分享卡片（可编辑分享标题/描述/封面图）
    │
    ├── 方式A: 直接转发给微信好友/群
    │      └── 朋友点击卡片 → 进入小程序客人展示页
    │
    ├── 方式B: 生成小程序码（方形/圆形）
    │      └── 保存到相册 → 印刷到实体请柬上
    │      └── 客人扫码 → 直接进入展示页
    │
    └── 方式C: 复制链接（备用）
           └── 浏览器打开 → 提示"在小程序中打开"
```

### 4.2 角色判断逻辑

```javascript
App.onLaunch() {
  // 1. 获取当前用户 OpenID
  const userOpenId = await wx.cloud.callFunction({ name: 'getOpenId' });

  // 2. 查询此 OpenID 是否创建了婚礼
  const myWedding = await db.collection('weddings').where({ ownerOpenId: userOpenId }).get();

  if (myWedding.data.length > 0) {
    // 是主人 → 进入工作台
    wx.switchTab({ url: '/pages/editor/index' });
  } else {
    // 3. 检查是否有访问密码（URL参数或本地存储）
    const accessCode = options.query.code || wx.getStorageSync('accessCode');

    if (accessCode) {
      // 验证密码
      const wedding = await db.collection('weddings').where({ accessCode }).get();
      if (wedding.data.length > 0) {
        // 是客人 → 进入展示页
        wx.redirectTo({ url: `/pages/invitation/show?id=${wedding.data[0]._id}` });
      } else {
        // 密码错误 → 提示输入密码
        wx.redirectTo({ url: '/pages/auth/login' });
      }
    } else {
      // 首次进入，无密码 → 引导创建或输入密码
      wx.redirectTo({ url: '/pages/auth/login' });
    }
  }
}
```

### 4.3 登录/创建流程页

```
pages/auth/login

┌─────────────────────────────────────┐
│                                     │
│         💍 婚礼导航                  │
│                                     │
│   「制作我的婚礼请柬」               │
│   ┌─────────────────────────┐      │
│   │    微信一键登录/创建     │      │
│   └─────────────────────────┘      │
│                                     │
│   ───────── 或 ─────────            │
│                                     │
│   「我有邀请密码」                   │
│   ┌─────────────────────────┐      │
│   │  ┌──┐ ┌──┐ ┌──┐ ┌──┐  │      │
│   │  │  │ │  │ │  │ │  │  │      │
│   │  └──┘ └──┘ └──┘ └──┘  │      │
│   └─────────────────────────┘      │
│                                     │
│   [输入6位数字密码进入请柬]          │
│                                     │
└─────────────────────────────────────┘
```

---

## 五、视觉设计方向

### 5.1 客人端：沉浸式展示美学

客人端是第一印象，必须足够精美，让收到的客人感到" wow "。

**设计原则：**
- **全屏大图**：封面和照片墙占满屏幕宽度，视觉冲击力强
- **字体层级**：标题用思源宋体（衬线），正文用思源黑体（无衬线）
- **留白呼吸**：段落之间 ample 留白，不要信息堆砌
- **微动效**：滚动视差、图片渐入、文字逐行浮现
- **色彩克制**：展示页以婚礼主色调为主，避免过多颜色干扰

**参考风格：**
- 韩国婚礼请柬小程序（简约留白 + 大图）
- 小红书婚礼笔记（精致排版 + 情感文案）
- Apple 产品页（滚动叙事 + 大图文字穿插）

### 5.2 主人端：高效编辑器美学

主人端是工具，要在美观和效率之间找平衡。

**设计原则：**
- **所见即所得**：编辑区与预览区实时联动
- **组件化面板**：编辑项分组明确，Tab切换
- **操作反馈**：保存成功 toast、加载骨架屏、提交确认
- **深色/浅色**：编辑器可用浅色背景减少视觉疲劳

---

## 六、功能清单（P0/P1/P2）

### P0 — 核心必须（MVP）

| 模块 | 功能 | 主人端 | 客人端 |
|------|------|--------|--------|
| 身份系统 | 微信登录 + OpenID 绑定 | ✅ | ✅ |
| 身份系统 | 6位数字访问密码 | ✅ | ✅ |
| 婚书编辑 | 3种模板切换 | ✅ | 展示 |
| 婚书编辑 | 新人姓名编辑 | ✅ | 展示 |
| 婚书编辑 | 婚礼日期/时间编辑 | ✅ | 展示 |
| 婚书编辑 | 婚礼地点搜索+地图选点 | ✅ | 展示 |
| 婚书编辑 | 6种文案预设 + 自定义 | ✅ | 展示 |
| 照片墙 | 上传婚纱照片（最多20张） | ✅ | 展示 |
| 照片墙 | 照片点击放大预览 | — | ✅ |
| RSVP | 姓名/电话/出席人数/祝福 | 查看统计 | ✅ 提交 |
| 分享 | 生成分享卡片 | ✅ | — |
| 分享 | 生成小程序码 | ✅ | — |
| 导航 | 一键唤起腾讯地图导航 | — | ✅ |

### P1 — 体验增强

| 模块 | 功能 | 说明 |
|------|------|------|
| 背景音乐 | 预设音乐 + 自定义上传 | 客人端可播放/暂停 |
| 照片墙 | 拖拽排序 | 主人端管理 |
| 照片墙 | 设置封面图 | 影响分享卡片 |
| RSVP统计 | 图表展示（饼图/柱状图） | 主人端数据可视化 |
| RSVP | 饮食偏好收集 | 素食/清真/忌海鲜等 |
| 时间线 | 婚礼当天流程展示 | 客人端了解安排 |
| 祝福留言 | 弹幕/卡片式展示 | 客人端互动 |
| 多管理员 | 新娘+新郎共同编辑 | 双方OpenID绑定 |

### P2 — 高级功能

| 模块 | 功能 | 说明 |
|------|------|------|
| 实时预览 | 二维码预览 | 编辑时用手机扫码实时看效果 |
| 数据导出 | RSVP导出Excel | 主人端一键导出 |
| 座位管理 | 座位图编辑 | 可视化排座位 |
| 礼金记录 | 电子礼金簿 | 记录收到的礼金 |
| AI文案 | 智能生成请柬文案 | 基于新人信息生成个性化文案 |
| 视频请柬 | 上传短视频作为封面 | 支持15秒视频 |

---

## 七、项目目录结构（小程序）

```
wedding-navigator-miniprogram/
├── cloudfunctions/           # 云函数
│   ├── getOpenId/            # 获取用户OpenID
│   ├── generateQRCode/       # 生成小程序码
│   ├── sendReminder/         # 发送 RSVP 提醒
│   └── exportExcel/          # 导出 RSVP Excel
│
├── miniprogram/
│   ├── app.js                # 应用入口（角色判断路由）
│   ├── app.json              # 全局配置
│   ├── app.wxss              # 全局样式（设计系统）
│   ├── pages/
│   │   ├── auth/
│   │   │   └── login         # 登录/创建/密码入口
│   │   ├── editor/
│   │   │   └── index         # 婚书编辑工作台（主人）
│   │   ├── gallery/
│   │   │   └── manage        # 照片墙管理（主人）
│   │   ├── preview/
│   │   │   └── index         # 实时预览（主人）
│   │   ├── guests/
│   │   │   └── index         # 宾客管理（主人）
│   │   ├── mine/
│   │   │   └── index         # 我的设置（主人）
│   │   └── invitation/
│   │       └── show          # 请柬展示页（客人，核心）
│   │
│   ├── components/
│   │   ├── invitation-card/    # 婚书渲染组件（3种风格）
│   │   ├── photo-grid/         # 照片墙网格
│   │   ├── photo-viewer/       # 图片预览器
│   │   ├── rsvp-form/          # RSVP表单
│   │   ├── countdown/          # 倒计时组件
│   │   ├── music-player/       # 背景音乐播放器
│   │   ├── share-card/         # 分享卡片生成
│   │   └── blessing-wall/      # 祝福留言墙
│   │
│   ├── utils/
│   │   ├── store.js            # MobX 全局状态
│   │   ├── api.js              # 云开发 API 封装
│   │   ├── date.js             # 日期工具
│   │   └── validators.js       # 表单校验
│   │
│   └── static/
│       ├── images/             # 本地图片资源
│       └── music/              # 预设背景音乐
│
├── project.config.json
└── README.md
```

---

## 八、关键技术决策

### 8.1 为什么不用 Taro / uni-app？

虽然跨端框架可以一套代码跑 H5 + 小程序，但本项目：
- 核心目标就是微信小程序（利用微信分享、扫码、云开发）
- 原生小程序的分享卡片、小程序码、云开发 SDK 是最顺滑的
- 避免引入框架层的抽象和兼容问题，减少包体积

### 8.2 云开发 vs 自建后端

| 维度 | 微信云开发 | 自建后端 |
|------|-----------|---------|
| 成本 | 免费额度够用（5GB存储+2GB数据库+5万次调用/月） | 需要服务器费用 |
| 运维 | 零运维，自动扩缩容 | 需要维护部署 |
| 开发速度 | 前端直接调 SDK，最快 | 需要写 API 接口 |
| 灵活性 | 适合标准 CRUD | 更灵活 |
| 微信生态 | 深度集成 | 需要额外接入 |

**结论**：云开发完全满足 MVP 需求，后续如有复杂需求再迁移。

### 8.3 照片存储策略

- 缩略图（展示用）：上传时自动生成压缩版本，云存储
- 原图（预览用）：上传原图，按需加载
- 封面图：单独字段存储，用于分享卡片
- 每张图限制 5MB，最多 20 张

---

## 九、风险评估与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 云开发免费额度不足 | 婚礼当天流量集中 | 提前购买资源包，设置告警 |
| 审核不通过 | 无法发布 | 避免敏感内容，提前提交审核 |
| 照片加载慢 | 客人体验差 | 压缩+CDN+懒加载+占位图 |
| 主人误操作删除数据 | 不可恢复 | 软删除机制 + 操作确认 |
| 多设备编辑冲突 | 数据覆盖 | 乐观锁 + 最后写入时间戳 |

---

## 十、开发阶段规划

```
Phase 1: 基础设施（2天）
├── 小程序项目初始化
├── 云开发环境配置
├── 数据库集合创建
├── 登录/角色判断流程
└── 全局样式/组件库引入

Phase 2: 客人端展示页（3天）
├── 请柬展示页长滚动结构
├── 婚书3种模板渲染组件
├── 照片墙 + 图片预览
├── 婚礼信息 + 一键导航
├── RSVP表单提交
└── 分享卡片配置

Phase 3: 主人端编辑器（3天）
├── 编辑工作台页面
├── 婚书实时预览联动
├── 照片上传/排序/管理
├── 婚礼信息表单
└── 模板/文案切换

Phase 4: 主人端管理（2天）
├── RSVP列表与统计
├── 宾客管理
├── 分享功能（卡片+小程序码）
└── 我的设置

Phase 5: 优化上线（2天）
├── 性能优化（图片懒加载、分包）
├── 真机测试
├── 微信审核提交
└── 使用文档
```

总工期预估：**12 个工作日**（约 2.5 周）

---

*本方案为设计阶段文档，不涉及代码实现。如需进入开发阶段，请确认方案后再开始编码。*
